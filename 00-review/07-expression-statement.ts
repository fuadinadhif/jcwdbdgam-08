// {} slot expression

/* ------------------------------- Expression ------------------------------- */
// Suatu kode yang memberikan sebuah value
2 + 3;
"Hi" + "There";
true && false;
myFunc();
true ? "true" : "false";

/* -------------------------------- Statement ------------------------------- */
// Sebuah instruksi
function myFunc() {
  return "myFunc";
}

let x = 2 + 3;

if (true) {
} else {
}
